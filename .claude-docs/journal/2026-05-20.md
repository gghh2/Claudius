# Journal — 2026-05-20

## Contexte

Grosse session : finalisation de la migration Unity 6.2, mise en place du système
de documentation, puis développement de features. Le projet venait d'être ouvert
sous Unity 6.2 (migration depuis Unity 2021.3.23f1).

## Fait

### Migration Unity 6.2 — finalisation
- Migration de l'API dans les scripts (`linearVelocity`/`linearDamping`,
  `FindFirstObjectByType` / `FindObjectsByType`), correction du nom des enums
  (`FindObjectsSortMode` / `FindObjectsInactive`).
- ~60 warnings nettoyés (`CS0618` / `CS0414` / `CS0219`).
- `QuestObject` : emojis → tags texte `[ITEM]`, `[NPC]`…
- `.gitattributes` (config Unity), `.gitignore` (`_Recovery`, `unity-bridge`,
  `ORIGINAL`), driver de merge `unityyamlmerge`. Commit `5cdfc43` + push.

### Système de documentation
- `CLAUDE.md` (hub) + `.claude-docs/` : `STATE` / `BACKLOG` / `FEATURES` / journal.
- Audit du code mort et des fonctionnalités non branchées → versé dans `BACKLOG.md`.
- `SPEC_LLM_local.md` : spécification du LLM embarqué.

### Feature AdventureJournal — mise en place complète
- `AdventureJournalUI` refondu en script UI per-scene ; `AdventureJournalPanelHelper`.
- Script éditeur `AdventureJournalPanelBuilder` (génère le panneau ;
  menu `Tools > Claudius`).
- Câblage des événements : quête acceptée / terminée, objet ramassé, zone
  découverte, 1ʳᵉ rencontre PNJ.
- Save/load du journal intégré à `SaveGameManager`.
- Bugs corrigés : constructeurs sans argument pour la désérialisation JSON ;
  GameObject `Content` du panneau désactivé ; `max_tokens` 150→400 (texte tronqué) ;
  prompt « station spatiale » → « planète alien ».
- Bouton « Rafraîchir » retiré (la narration est automatique).

### Corrections diverses
- `SceneNavigationManager` : se détache à la racine avant `DontDestroyOnLoad`.
- Curseur caché dans le panneau Save/Load + curseur bloqué dans l'éditeur :
  `SmartCursorManager` délègue désormais à `UnifiedUIManager.IsShowingPanel`.
- Curseur perdu au clic « Quitter » : flag `isQuitting` dans `PauseMenuUI`.
- Confirmation avant de quitter / de retourner au menu principal.

### Abstraction du moteur IA
- Créé `IAIProvider` + `OpenAIProvider` + `MockAIProvider` + `AIService`.
- `AIDialogueManager` et `AdventureJournalUI` migrés : plus aucun appel OpenAI
  en dur. Changer de backend = réassigner `AIService.Provider`.

### Consolidation du curseur
- `SmartCursorManager` réécrit en contrôleur à responsabilité unique, piloté par
  `UnifiedUIManager.IsShowingPanel`. `UnifiedUIManager` et `PauseMenuUI` ne
  touchent plus au curseur (−265 lignes). Le « bug » ESC-ESC s'est révélé être un
  artefact de l'éditeur Unity, pas du code.

### Passe « bonnes pratiques »
- Lecture des e-books Unity (C# style guide, architecture ScriptableObjects) →
  audit consigné dans `.claude-docs/AUDIT_CODE.md`. Verdict : projet sain ; cible
  ScriptableObject concrète identifiée = Runtime Set (remplacer les `FindObjectsByType`).

### Commits de la session
`5cdfc43` (migration Unity 6), `cf3cff4` (AdventureJournal + abstraction IA +
système de doc), `5a1843a` (consolidation curseur).

## Décisions en attente

- Sort du sous-système `DynamicAssets` (reprendre / geler).

## Prochaine session

- Suite du LLM embarqué (`SPEC_LLM_local.md`) ou `BACKLOG.md` (bugs quêtes,
  passe « bonnes pratiques »).
